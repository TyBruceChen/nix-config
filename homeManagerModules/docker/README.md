Stored at .config/docker/<name>/
Mounted volumes can be checked through: 
    sudo docker volumes ls
By specify the volume name, the mounted path can be checked by:
    sudo docker volumes inspect <volume name>
